# Acceptance Criteria

The project is considered successful if:

- Customer authentication is completed successfully.
- User requests are routed to the correct service.
- Backend APIs return valid responses.
- Policy information is displayed correctly.
- Claims are created successfully.
- Update requests are stored and tracked.
- Failed authentication triggers retry logic.
- After maximum retries, the customer is transferred to a live support agent with conversation context.
